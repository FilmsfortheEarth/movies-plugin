# ===================================
#  List Column Definitions
# ===================================

columns:
    title:
        label: Title
        type: text
    url:
        label: Url
        type: text
    provider:
        label: Provider
        relation: provider
        sortable: false
        select: name
    format:
        label: Format
        relation: format
        sortable: false
        select: name