# ===================================
#  List Column Definitions
# ===================================

columns:
    movie:
        label: Movie
        relation: movie
        select: title
    title:
        label: Title
        type: text
    code:
        label: Code
        type: text
    provider:
        label: Provider
        relation: provider
        select: name
    format:
        label: Format
        relation: format
        select: name
        default: NULL