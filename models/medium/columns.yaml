# ===================================
#  List Column Definitions
# ===================================

columns:
    movie:
        label: Movie
        relation: movie
        select: title
    title:
        label: Title
        type: text
    url:
        label: Url
        type: text
    provider:
        label: Provider
        relation: provider
        sortable: false
        select: name
    format:
        label: Format
        relation: format
        sortable: false
        select: name
        default: NULL